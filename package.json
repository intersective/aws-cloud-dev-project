{
    "name": "cloud-developer-project",
    "version": "1.0.0",
    "description": "DynamoDB CRUD API using API Gateway and AWS Lambda",
    "main": "api.js",
    "scripts": {
        "build": "tsc --build",
        "clean": "tsc --build --clean",
        "lint": "npx eslint --quiet src/.",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "Jackson Yuan / Practera",
    "license": "ISC",
    "dependencies": {
      "@aws-sdk/client-dynamodb": "^3.18.0",
      "@aws-sdk/util-dynamodb": "^3.18.0",
      "typescript": "^5.0.4"
    },
    "devDependencies": {
        "@types/node": "^18.16.0",
        "@typescript-eslint/eslint-plugin": "^5.59.1",
        "@typescript-eslint/parser": "^5.59.1",
        "eslint": "^8.39.0",
        "serverless-iam-roles-per-function": "^3.2.0",
        "typescript": "^5.0.4"
    }
}